apiVersion: v1
kind: Service
metadata:
  name: {{ template "identitydb.fullname" . }}
  labels:
    app: {{ template "identitydb.name" . }}
    chart: {{ template "identitydb.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "identitydb.name" . }}
    release: {{ .Release.Name }}
